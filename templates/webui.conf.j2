#{{ ansible_managed }}
Console {
    Name = {{ item.name }}
    Password = "{{ item.password | default() }}"
    Profile = "{{ item.profile }}"
    TlsEnable = {{ item.tls_enabled | default("false") }}
}
