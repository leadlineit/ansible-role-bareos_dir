#{{ ansible_managed }}
Job {
    Name = "{{ item.name }}"
    Description = "{{ item.description | default(item.name) }}"
    JobDefs = {{ item.jobdef }}
    Client = {{ item.client }}
    Enabled = yes
    Run Script {
       Console = ".bvfs_update jobid=%i"
       RunsWhen = After
       RunsOnClient = No
    }
}
