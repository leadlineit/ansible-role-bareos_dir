---
# handlers file for mysql_server

- name: enable bareos-dir service
  service:
    name: bareos-dir
    enabled: true

- name: restart bareos-dir service
  service:
    name: bareos-dir
    state: restarted
